apiVersion: v1
kind: Service
metadata:
  name: hras-frontend
  namespace: hras
  labels:
    app.kubernetes.io/name: hras-frontend
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: hras-frontend
