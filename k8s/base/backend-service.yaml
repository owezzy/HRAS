apiVersion: v1
kind: Service
metadata:
  name: hras-backend
  namespace: hras
  labels:
    app.kubernetes.io/name: hras-backend
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: hras-backend
